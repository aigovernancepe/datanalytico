---
// Google Analytics 4 (GA4) integration
// GA4 scripts are loaded dynamically by CookieBanner.astro after user consent.
// This component is kept as a reference for the measurement ID.
const GA_MEASUREMENT_ID = 'G-TDKCGD4C7L';
---

<!-- GA4 measurement ID (scripts loaded after cookie consent via CookieBanner) -->
<script is:inline define:vars={{ GA_MEASUREMENT_ID }}>
  window.GA_MEASUREMENT_ID = GA_MEASUREMENT_ID;
</script>
